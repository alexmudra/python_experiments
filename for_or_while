# Напишіть цикл for, який виведе на екран всі парні числа від 2 до 10.
# #
# # Напишіть цикл while, який виведе на екран всі непарні числа від 1 до 9.
# #
# # Напишіть цикл for, який виведе на екран всі літери в рядку "Hello, World!".
# #
# # Напишіть цикл while, який виведе на екран таблицю множення на 5 з числами від 1 до 10.
# #
# # Напишіть цикл for, який пройде по списку my_list та виведе на екран всі елементи списку, які є числами.
# #
# # Напишіть цикл while, який зчитує введене користувачем число, та продовжує запитувати число, поки введене число не буде менше 10.


# Напишіть цикл for, який виведе на екран всі парні числа від 2 до 10.
for numb in range(2, 11, 2):
    print(numb)

# Напишіть цикл while, який виведе на екран всі непарні числа від 1 до 9.
n = 1
while n <= 9:
    print(n)
    n = n+2

 # Напишіть цикл for, який виведе на екран всі літери в рядку "Hello, World!".
h = "Hello, World!"
for i in h:
    print(i)


# Напишіть цикл while, який виведе на екран таблицю множення на 5 з числами від 1 до 10

m = 1

while m <= 10:
    j = m * 5
    print("Result",j)
    m = m+1


# Напишіть цикл for, який пройде по списку my_list та виведе на екран всі елементи списку, які є числами
my_list = [1, 2,True, 2, 3, 4,"sdf", 6.1, 7]
for i in my_list:

    if isinstance(i, (int, float)):
        print(i)


# Напишіть цикл while, який зчитує введене користувачем число, та продовжує запитувати число, поки введене число не буде менше 10.

# while True:
#     n = int(input("Enter number "))
#     if n < 10:
#         break

# Завдання на цикли for:
#
# Вивести на екран перші 10 чисел послідовності Фібоначчі.
#
# Перевірити, чи міститься задане слово у реченні, використовуючи цикл for.
#
# Підрахувати кількість парних та непарних чисел у заданому списку за допомогою циклу for.
#
# Вивести на екран всі позитивні елементи заданого списку цілих чисел за допомогою циклу for.
#
# Створити програму, яка буде виводити на екран літери, які зустрічаються у реченні більше одного разу.
#
# Завдання на цикли while:
#
# Створити програму, яка знаходить суму чисел, що вводяться користувачем, доки користувач не введе 0.
#
# Створити програму, яка буде виводити на екран всі непарні числа, які знаходяться в заданому діапазоні.
#
# Вивести на екран таблицю множення заданого числа за допомогою циклу while.
#
# Створити програму, яка буде запитувати у користувача пароль доти, доки він не введе правильний пароль.
#
# Виконати сортування заданого списку цілих чисел за зростанням, використовуючи цикл while.


# Перевірити, чи міститься задане слово у реченні, використовуючи цикл for.

st = "Забезпечує стабільну роботу віскомуфти і запобігає перегріванню двигуна. Рекомендується для профілактики і ремонту автотранспорту."
substrings = st.split()
substr = "ремонту"

for i in substrings:
    if substr in i:
        print("Substring found")
        break
else:
    print("Substring not found")


# Підрахувати кількість парних та непарних чисел у заданому списку за допомогою циклу for
spusok  = [i for i in range(0, 35)]
ev_numb = 0
not_ev_numb = 0
for x in spusok:
    if x % 2 == 0:
        ev_numb = ev_numb+1
    else:
        not_ev_numb = not_ev_numb + 1

print(ev_numb)
print(not_ev_numb)


# Вивести на екран всі позитивні елементи заданого списку цілих чисел за допомогою циклу for.
#вар 1
op = [i for i in range(-10, 35)]

for x in op:
    if isinstance(x, (int)) and x >= 0:
        print("Pos number", x)

#вар 2
op = [i for i in range(-10, 35) if i >= 0] #в генераторі списків задав умову на позитивні числа
print("Positive numbers:", op)


# # Створити програму, яка буде виводити на екран літери, які зустрічаються у реченні більше одного разу.
#АЛГОРИТМ
# Створити порожній список для збереження літер, які зустрічаються більше одного разу.
# Розбити речення на окремі слова та зберегти їх у списку.
# Для кожного слова перетворити його на множину літер (set) та зберегти у окремому списку.
# Об'єднати всі списки літер у один великий список.
# Створити порожній словник для збереження кількості входжень кожної літери.
# Для кожної літери у великому списку порахувати кількість входжень та зберегти у словнику.
# Для кожної літери у словнику перевірити, чи є кількість входжень більше одного, якщо так, то зберегти цю літеру у списку з кількістю входжень більше одного.
# Вивести на екран список літер, які зустрічаються більше одного разу.

phrase = "Забезпечує стабільну роботу віскомуфти і запобігає перегріванню двигуна. Рекомендується для профілактики і ремонту автотранспорту."
lett = phrase.split()
sets = []

for word in lett:
    letter_set = set(word) # перетворюємо слово на множину літер
    sets.append(letter_set) # додаємо множину до списку
# print(sets)
letters = [] # Створення великого списку

for letter_set in sets:
    letters.extend(letter_set)
# print(letters)
char_dict = {}
for word in letters:
    for char in word:
        if char in char_dict:
            char_dict[char] += 1
        else:
            char_dict[char] = 1

# Створення списку з літерами, які зустрічаються більше одного разу
multi_char_list = []
for char in char_dict:
    if char_dict[char] > 1:
        multi_char_list.append(char)

# Виведення результатів
print('Великий список:', letters)
print('Словник літер та їх кількостей:', char_dict)
print('Список літер, які зустрічаються більше одного разу:', multi_char_list)